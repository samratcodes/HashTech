const config = {
  plugins: ["@tailwindcss/postcss"],
};

export default config;

// To customize Tailwind colors, create a tailwind.config.js file in the project root.
// Example: module.exports = { theme: { extend: { colors: { primary: '#4169e1', accent: '#FFA500' } } } }
